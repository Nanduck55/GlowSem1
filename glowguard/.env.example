# ── GlowGuard environment ──────────────────────────────────────────
# Copy this file to `.env` and adjust for your setup.

# Where your PHP backend lives (XAMPP example):
 VITE_API_URL=http://localhost/GlowSem1/glowguard-backend

# Keep the app fully functional WITHOUT a backend (browser localStorage):
VITE_USE_MOCK=true
